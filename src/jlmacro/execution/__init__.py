"""Execution: broker abstraction (BaseBroker) and PaperBroker.

Live trading is disabled by default (see jlmacro.config.Settings.jlmacro_live_trading_enabled)
and must remain so until explicit human approval and broker-specific safeguards are
implemented, per the platform's execution safety requirements. Populated from Phase 10.
"""
